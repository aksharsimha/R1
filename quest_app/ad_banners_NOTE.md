# Sponsor Ad Banners Configuration Note

> **IMPORTANT:** The `click_url` values configured in [`quest_app/ad_banners.json`](file:///c:/Users/DELL/Downloads/R1-github/quest_app/ad_banners.json) must be swapped for actual affiliate/referral links once a real partnership is set up with each platform (e.g., Groww's referral program, Zerodha Partner program, etc.).

A plain link to a homepage generates no revenue on its own.
